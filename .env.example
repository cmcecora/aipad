# Sync Server Configuration
# For physical devices on the same network, set this to your computer's IP address
# Example: EXPO_PUBLIC_SYNC_SERVER_URL=ws://192.168.1.100:3001/sync
# For simulator/emulator, you can leave it unset (will auto-detect)
EXPO_PUBLIC_SYNC_SERVER_URL=